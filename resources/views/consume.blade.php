<!DOCTYPE html>
<html>
<head>
    <title>Consume API</title>
</head>
<body>
    {{-- <img src="{{ $message }}" alt="" srcset=""> --}}
    {{-- @foreach ($info as $item)
        <li>{{ $item['type'] }}</li>
    @endforeach --}}
    <h3>{{ $type }}</h3>
    <p>{{ $setup }}</p>

    {{-- <p>{{ $type }}</p> --}}
</body>
</html>
